interface:
  display_name: "Composition-First Refactor"
  short_description: "Sketch React data and component architecture before refactoring"
  default_prompt: "Use $composition-first-refactor to review this target, ask architecture questions, sketch data flow and component tree, then wait for approval before coding."

policy:
  allow_implicit_invocation: true
